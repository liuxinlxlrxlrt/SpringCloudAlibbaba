spring:
  cloud:
    nacos:
      discovery:
        ###服务注册地址
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
  application:
    name: mayikt-order
  profiles:
    active: pre
server:
  port: 9091

mayikt:
  name: new_mayikt